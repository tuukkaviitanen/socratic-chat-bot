name: socratic-chat-bot-dev
services:
  main:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 8080:8080
    volumes:
      - ./src:/app/src
      - ./templates:/app/templates
      - ./static:/app/static
